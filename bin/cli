#! /usr/bin/env node

require('dotenv').config();

const program = require('commander');

program
  .version('0.0.1')
  .command('list', 'list names and states of event rules')
  .command('view', 'view rule details')
  .command('edit', 'update rule config')
  .parse(process.argv);
