const assert = require('assert');

describe('handler', function() {
  it('it gets initial results from the ap endpoint', function() {
  });

  it('it updates existing results using `nexturl`', function() {

  });

  it('throws if an election date is not specified')
});
